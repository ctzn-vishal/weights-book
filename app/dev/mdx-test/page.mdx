# MDX pipeline test

Median household income was $74,580 in 2022 and a coffee costs $3.50, and neither should render as math.

The final weight is $$w_i = d_i\,g_i$$ and the Kish effective size is $$\nKish = n/(1+\CV(w)^2)$$.

$$
\hat{\theta} = \frac{\sum_{i \in s} w_i\, y_i}{\sum_{i \in s} w_i}, \qquad \DEFF(\hat\theta) = \frac{\Var_{\text{design}}(\hat\theta)}{\Var_{\text{SRS}}(\hat\theta)}
$$

| unit | weight |
|---|---|
| household | HHWT |
| person | PERWT |

{/* an MDX comment survives compilation */}
